/**
 * Visual Area Component
 * The top 70% of the game layout, displays backgrounds, sprites, and scene art
 */

export default function VisualArea() {
  return (
    <div className="visual-area">
      {/* Future: Background images, character sprites, scene art */}
      {/* For now: empty themed container */}
    </div>
  );
}
